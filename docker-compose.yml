services:
  dapple:
    image: afraley/dapple:latest
    container_name: dapple
    restart: unless-stopped
    ports:
      - "${DAPPLE_PORT:-8080}:8080"
    volumes:
      - ./data:/data:z
    environment:
      DAPPLE_UID: "${DAPPLE_UID:-1000}"
      DAPPLE_GID: "${DAPPLE_GID:-1000}"
